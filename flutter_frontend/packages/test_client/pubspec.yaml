name: test_client
description: Dart-only test client for libmsgr
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.5.1

dependencies:
  libmsgr:
    path: ../libmsgr
  logging: ^1.2.0
  args: ^2.5.0

dev_dependencies:
  lints: ^4.0.0
